#TODO: first check is CNN obtain more problem's charistic, if so, do implemnt here
#we already break text structure with BoW so CNN might also only improve very little, try to add text structure to feature instead
